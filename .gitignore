# Mirrored Claude Code transcripts — these contain YOUR session content.
# Never commit them.
sessions/
*.jsonl
*.cwd.txt

# Local config (contains real paths). The .example is tracked.
journal.config

# Obsidian local state (if you keep a vault inside the repo for testing)
.obsidian/workspace*
.obsidian/cache

# OS / editor cruft
.DS_Store
Thumbs.db
node_modules/
